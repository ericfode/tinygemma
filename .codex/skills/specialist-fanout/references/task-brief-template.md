# Task Brief Template

Use a brief like this:

```text
Goal: <one bounded outcome>
Owned paths: <exact files or directories>
Do not edit: <everything outside the owned paths>
Constraints: <tests, style, portability, runtime assumptions>
Output: changed files + unresolved issue
```

If the task is read-only, replace `Owned paths` with the exact question to answer.
